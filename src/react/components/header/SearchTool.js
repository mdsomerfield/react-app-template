import React from 'react';

import css from './SearchTool.scss';

const SearchTool = () => (
  <div className={css.container}>
    <input className={css.input} />
    <button className={css.button} />
  </div>
);

export default SearchTool;