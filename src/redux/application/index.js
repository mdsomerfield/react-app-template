import reducers from './reducers';

import duckConstants from './constants';
import duckOperations from './operations';
import duckSelectors from './selectors';

export const constants = duckConstants;
export const operations = duckOperations;
export const selectors = duckSelectors;

export default reducers;
